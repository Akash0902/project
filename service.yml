apiVersion: v1
kind: Service
metadata:
  name: project-svc
  namespace: project
spec:
  type: NodePort
  selector:
    app: project
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30083
